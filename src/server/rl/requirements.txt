# CyberX RL Environment – Python Dependencies
# Install with: pip install -r requirements.txt

# ── Core RL stack ──────────────────────────────────────────────────────────────
gymnasium==0.29.1
stable-baselines3==2.3.2
sb3-contrib==2.3.0            # RecurrentPPO (LSTM policy)
torch>=2.1.0                  # GPU training; install the CUDA build manually if needed

# ── Numerical / scientific ─────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                 # statistical significance tests in evaluator

# ── Visualisation ──────────────────────────────────────────────────────────────
matplotlib>=3.8.0
tensorboard>=2.15.0           # live training curves (tensorboard --logdir ./logs)

# ── Data & utilities ───────────────────────────────────────────────────────────
pandas>=2.1.0
tqdm>=4.66.0

# ── API server (Flask) ─────────────────────────────────────────────────────────
flask>=3.0.0
flask-cors>=4.0.0

# ── LLM Oracle (HTTP clients) ─────────────────────────────────────────────────
requests>=2.31.0
# anthropic>=0.26.0            # optional: uncomment if using Anthropic API directly

# ── Optional: experiment tracking ─────────────────────────────────────────────
 wandb>=0.17.0                # uncomment if you want Weights & Biases logging
# optuna>=3.6.0                # uncomment if you want hyperparameter search